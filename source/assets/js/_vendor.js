;(function(window, document, $, undefined) {
	'use strict';

	/**
	 * Example: Use mustache syntax for underscore templates
	 */
	// _.templateSettings = {
	// 	escape:      /\{-(.+?)\}/g,
	// 	evaluate:    /\{\{#(.+?)\}\}/g,
	// 	interpolate: /\{\{(?!#)(.+?)\}\}/g
	// };

})(window, document, jQuery);